<Window x:Class="SolarRent.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SolarRent - Система управления"
        Height="900" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#2E2EFF"/>
        <SolidColorBrush x:Key="GrayBackground" Color="#E8E8E8"/>

        <!-- Стиль верхней панели -->
        <Style x:Key="HeaderBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="Padding" Value="20,15"/>
        </Style>

        <Style x:Key="HeaderTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Кнопки быстрых действий (в правой части заголовка) -->
        <Style x:Key="HeaderButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#2E2EFF"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F0FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль вкладок навигации (как в старом варианте с TabControl) -->
        <Style x:Key="NavigationTabItemStyle" TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="25,12"/>
            <Setter Property="BorderThickness" Value="0,0,0,3"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"
                                      TextElement.Foreground="{TemplateBinding Foreground}"
                                      TextElement.FontWeight="{TemplateBinding FontWeight}"
                                      TextElement.FontSize="{TemplateBinding FontSize}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="#2E2EFF"/>
                                <Setter Property="BorderBrush" Value="#2E2EFF"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ВЕРХНЯЯ СИНЯЯ ПАНЕЛЬ (заголовок + быстрые действия) -->
        <Border Grid.Row="0" Style="{StaticResource HeaderBorderStyle}" Height="70">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="PageTitleText" Text="Каталог оборудования" 
                   Style="{StaticResource HeaderTitleStyle}"
                   VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="ActionButtonsPanel" VerticalAlignment="Center"/>
            </Grid>
        </Border>
        
        <!-- ПАНЕЛЬ ВКЛАДОК (навигация) -->
        <TabControl Grid.Row="1" 
                    x:Name="NavigationTabControl"
                    Background="White"
                    BorderBrush="#E0E0E0"
                    BorderThickness="0,0,0,1"
                    SelectionChanged="NavigationTabControl_SelectionChanged">
            <TabControl.Resources>
                <Style TargetType="TabPanel">
                    <Setter Property="HorizontalAlignment" Value="Left"/>
                    <Setter Property="Margin" Value="20,0,0,0"/>
                </Style>
            </TabControl.Resources>

            <TabItem Header="Каталог" Style="{StaticResource NavigationTabItemStyle}" Tag="Catalog"/>
            <TabItem Header="Календарь аренды" Style="{StaticResource NavigationTabItemStyle}" Tag="Calendar"/>
            <TabItem Header="Отчёты" Style="{StaticResource NavigationTabItemStyle}" Tag="Reports"/>
            <TabItem Header="Клиенты" Style="{StaticResource NavigationTabItemStyle}" Tag="Clients"/>
            <TabItem Header="Настройки" Style="{StaticResource NavigationTabItemStyle}" Tag="Settings"/>
            <TabItem Header="Продажа" Style="{StaticResource NavigationTabItemStyle}" Tag="Sale"/>
            <TabItem Header="Выдача аренды" Style="{StaticResource NavigationTabItemStyle}" Tag="LeaseIssue"/>
            <TabItem Header="Приемка аренды" Style="{StaticResource NavigationTabItemStyle}" Tag="LeaseAcceptance"/>
        </TabControl>

        <!-- ОСНОВНОЙ КОНТЕНТ (Frame) -->
        <Frame Grid.Row="2" x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>